# Tacokumo Admin API - Environment Variables Template
# Copy this file to .env and fill in the required values

# GitHub OAuth Configuration
# Get these values from your GitHub OAuth App settings
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# GitHub OAuth Callback URL (should match your OAuth App settings)
# For local development, this should be: http://localhost:8080/v1alpha1/auth/callback
GITHUB_CALLBACK_URL=http://localhost:8080/v1alpha1/auth/callback

# Allowed GitHub Organizations (comma-separated list)
# Users must be members of at least one of these organizations to access the API
GITHUB_ALLOWED_ORGS=your-org-1,your-org-2

# Frontend URL (for CORS and redirects)
# For local development, this should be: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Session TTL (Time To Live)
# How long user sessions should last (e.g., 24h, 1h, 30m)
SESSION_TTL=24h

# Development Settings
# Log level for debugging (debug, info, warn, error)
LOG_LEVEL=debug

# Database Settings (these are set in docker-compose by default)
# Only modify if you're using external database
# DB_HOST=postgresql
# DB_PORT=5432
# DB_USER=admin_api
# DB_PASSWORD=password
# DB_NAME=tacokumo_admin_db

# Redis Settings (these are set in docker-compose by default)
# Only modify if you're using external Redis instance
# REDIS_HOST=valkey
# REDIS_PORT=6379

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and fill in your GitHub OAuth app credentials
# 3. Update GITHUB_ALLOWED_ORGS with your organization names
# 4. Run: make docker-compose-up
