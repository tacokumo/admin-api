if you apply any changes to this project, run the following command to successfully build the project:

```bash
make generate # to generate API code and sqlc files
make # to format/test/build the project
```